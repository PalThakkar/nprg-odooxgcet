generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id           String       @id @default(uuid())
  loginId      String?      @unique
  email        String       @unique
  password     String
  name         String?
  phone        String?
  address      String?
  employeeId   String?      @unique
  companyId    String?
  roleId       String
  role         Role     @relation(fields: [roleId], references: [id])
  isFirstLogin Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  status       String?  @default("absent")
  company      Company? @relation(fields: [companyId], references: [id])

  data          AppData[]
  attendances   Attendance[]
  leaveRequests LeaveRequest[]
  salaryInfo    SalaryInfo?
  privateInfo   PrivateInfo?
  notifications Notification[]
  leaveBalance  LeaveBalance?
  payrolls      Payroll[]

  @@map("users")
}

model Notification {
  id        String   @id @default(uuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  type      String   @default("ANNOUNCEMENT") // ANNOUNCEMENT, SYSTEM, ALERT
  title     String
  message   String
  isRead    Boolean  @default(false)
  createdAt DateTime @default(now())

  @@map("notifications")
}

model Attendance {
  id       String    @id @default(uuid())
  userId   String
  user     User      @relation(fields: [userId], references: [id])
  checkIn  DateTime  @default(now())
  checkOut   DateTime?
  workHours  Float?
  extraHours Float?
  status     String    @default("present")
  date       DateTime  @default(now()) @db.Date

  @@map("attendances")
}

model Company {
  id            String            @id @default(uuid())
  name          String
  initials      String // For Login ID generation: "OI" -> Odoo India
  logoUrl       String?
  users         User[]
  counters      EmployeeCounter[]
  createdAt     DateTime          @default(now())
  updatedAt     DateTime          @updatedAt
  leaveRequests LeaveRequest[]

  @@map("companies")
}

model EmployeeCounter {
  id        String  @id @default(uuid())
  companyId String
  year      Int
  count     Int     @default(0)
  company   Company @relation(fields: [companyId], references: [id])

  @@unique([companyId, year])
  @@map("employee_counters")
}

model Role {
  id          String  @id @default(uuid())
  name        String  @unique
  description String?
  users       User[]

  @@map("roles")
}

model LeaveRequest {
  id           String      @id @default(uuid())
  userId       String
  user         User        @relation(fields: [userId], references: [id])
  companyId    String?
  company      Company?    @relation(fields: [companyId], references: [id])
  type         String // e.g. "SICK", "VACATION", "PERSONAL"
  leaveType    String? // e.g. "paid", "sick", "unpaid"
  startDate    DateTime
  endDate      DateTime
  status       String      @default("PENDING") // Using string for flexibility with PENDING/APPROVED/REJECTED or pending/approved/rejected
  reason       String?
  adminComment String?
  remarks      String?     // For compatibility with some API routes
  approvedBy   String?
  approvedAt   DateTime?
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt

  @@map("leave_requests")
}


model SalaryInfo {
  id           String @id @default(uuid())
  userId       String @unique
  user         User   @relation(fields: [userId], references: [id])
  monthlyWage  Float
  yearlyWage   Float
  workingDays  Int    @default(5)
  workingHours Float  @default(8)
  breakTime    Float  @default(1) // in hours

  // Salary Components
  basic             Float
  hra               Float
  standardAllowance Float
  performanceBonus  Float
  lta               Float
  fixedAllowance    Float

  // Deductions
  pfEmployee      Float
  pfEmployer      Float
  professionalTax Float @default(200)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("salary_info")
}

model PrivateInfo {
  id             String    @id @default(uuid())
  userId         String    @unique
  user           User      @relation(fields: [userId], references: [id])
  
  // Personal Info
  dateOfBirth    DateTime?
  residingAddress String?
  nationality    String?
  personalEmail  String?
  gender         String?
  maritalStatus  String?
  dateOfJoining  DateTime?
  
  // Bank Details
  accountNumber  String?
  bankName       String?
  ifscCode       String?
  panNo          String?
  uanNo          String?
  empCode        String?

  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt

  @@map("private_info")
}

model AppData {
  id        String   @id @default(uuid())
  content   String
  userId    String
  createdAt DateTime @default(now())
  user      User     @relation(fields: [userId], references: [id])

  @@map("app_data")
}

model LeaveBalance {
  id               String @id @default(uuid())
  userId           String @unique
  paidDaysLeft     Int    @default(24)
  sickDaysLeft     Int    @default(7)
  unpaidDaysLeft   Int    @default(0)
  year             Int    @default(2025)
  updatedAt        DateTime @updatedAt
  user             User   @relation(fields: [userId], references: [id])

  @@map("leave_balances")
}

model Payroll {
  id            String   @id @default(uuid())
  userId        String
  basicSalary   Float
  allowances    Float    @default(0)
  deductions    Float    @default(0)
  netSalary     Float
  payPeriod     String   // e.g., "2025-01"
  status        String   @default("draft") // draft, processed, paid
  processedAt   DateTime?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  user          User     @relation(fields: [userId], references: [id])

  @@map("payrolls")
}
